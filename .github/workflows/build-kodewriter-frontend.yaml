name: Build Kodewriter Frontend Image

on:
  push:
    branches:
      - main
    paths:
      - images/ai-agent/coding-agent/kodewriter-frontend/**
      - .github/workflows/build-kodewriter-frontend.yaml
  workflow_dispatch:

jobs:
  build:
    name: Build & Push Kodewriter Frontend
    runs-on: [self-hosted]
    timeout-minutes: 30

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Get version
        id: get_version
        run: echo "VERSION=$(cat images/ai-agent/coding-agent/kodewriter-frontend/VERSION)" >> $GITHUB_OUTPUT

      - name: Log in to Harbor Docker registry
        run: |
          echo "${{ secrets.HARBOR_PASSWORD }}" | \
            docker login harbor.michaelhomelab.work \
              --username "${{ secrets.HARBOR_USERNAME }}" \
              --password-stdin

      - name: Build Docker image
        run: |
          docker build \
            -t harbor.michaelhomelab.work/homelab/ai-agent/coding-agent/kodewriter-frontend:latest \
            -t harbor.michaelhomelab.work/homelab/ai-agent/coding-agent/kodewriter-frontend:${{ steps.get_version.outputs.VERSION }} \
            images/ai-agent/coding-agent/kodewriter-frontend

      - name: Push Docker image
        run: |
          for tag in latest ${{ steps.get_version.outputs.VERSION }}; do
            for i in 1 2 3 4 5; do
              echo "Attempt $i: Pushing kodewriter-frontend:$tag...";
              docker push harbor.michaelhomelab.work/homelab/ai-agent/coding-agent/kodewriter-frontend:$tag && break || sleep 15;
            done
          done

      - name: Clean up Docker image
        if: always()
        run: |
          docker rmi harbor.michaelhomelab.work/homelab/ai-agent/coding-agent/kodewriter-frontend:latest || true
          docker rmi harbor.michaelhomelab.work/homelab/ai-agent/coding-agent/kodewriter-frontend:${{ steps.get_version.outputs.VERSION }} || true

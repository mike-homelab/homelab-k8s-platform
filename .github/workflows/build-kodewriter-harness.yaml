name: Build Kodewriter Harness Image

on:
  push:
    branches:
      - main
    paths:
      - images/ai-agent/coding-agent/kodewriter-harness/**
      - .github/workflows/build-kodewriter-harness.yaml
  workflow_dispatch:

jobs:
  build:
    name: Build & Push Kodewriter Harness
    runs-on: [self-hosted]
    timeout-minutes: 30

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Get version
        id: get_version
        run: echo "VERSION=$(cat images/ai-agent/coding-agent/kodewriter-harness/VERSION)" >> $GITHUB_OUTPUT

      - name: Log in to Harbor Docker registry
        run: |
          echo "${{ secrets.HARBOR_PASSWORD }}" | \
            docker login harbor.michaelhomelab.work \
              --username "${{ secrets.HARBOR_USERNAME }}" \
              --password-stdin

      - name: Build Docker image
        run: |
          docker build \
            -t harbor.michaelhomelab.work/homelab/ai-agent/coding-agent/kodewriter-harness:latest \
            -t harbor.michaelhomelab.work/homelab/ai-agent/coding-agent/kodewriter-harness:${{ steps.get_version.outputs.VERSION }} \
            images/ai-agent/coding-agent/kodewriter-harness

      - name: Push Docker image
        run: |
          for tag in latest ${{ steps.get_version.outputs.VERSION }}; do
            for i in 1 2 3 4 5; do
              echo "Attempt $i: Pushing kodewriter-harness:$tag...";
              docker push harbor.michaelhomelab.work/homelab/ai-agent/coding-agent/kodewriter-harness:$tag && break || sleep 15;
            done
          done

      - name: Clean up Docker image
        if: always()
        run: |
          docker rmi harbor.michaelhomelab.work/homelab/ai-agent/coding-agent/kodewriter-harness:latest || true
          docker rmi harbor.michaelhomelab.work/homelab/ai-agent/coding-agent/kodewriter-harness:${{ steps.get_version.outputs.VERSION }} || true

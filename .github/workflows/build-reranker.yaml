name: Build Reranker Image

on:
  push:
    branches:
      - main
    paths:
      - images/ai-platform/reranker/**
      - .github/workflows/build-reranker.yaml
  pull_request:
    branches:
      - main
    paths:
      - images/ai-platform/reranker/**
      - .github/workflows/build-reranker.yaml
  workflow_dispatch:

jobs:
  build:
    name: Build & Push Reranker
    runs-on: [self-hosted]

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Read version
        id: version
        run: |
          VERSION=$(cat images/ai-platform/reranker/VERSION)
          echo "version=${VERSION}" >> $GITHUB_OUTPUT

      - name: Log in to Nexus Docker registry
        run: |
          echo "${{ secrets.NEXUS_PASSWORD }}" | \
            docker login docker.michaelhomelab.work \
              --username "${{ secrets.NEXUS_USERNAME }}" \
              --password-stdin

      - name: Build Docker image
        run: |
          docker build \
            -t docker.michaelhomelab.work/homelab-docker-repo/ai-platform/reranker:${{ steps.version.outputs.version }} \
            images/ai-platform/reranker

      - name: Push Docker image
        run: |
          docker push docker.michaelhomelab.work/homelab-docker-repo/ai-platform/reranker:${{ steps.version.outputs.version }}

name: Build Kito Artifact Generator Image

on:
  push:
    branches:
      - main
    paths:
      - images/ai-application/kito/artifact-generator/**
      - .github/workflows/build-artifact-generator.yaml
  workflow_dispatch:

jobs:
  build:
    name: Build & Push Kito Artifact Generator
    runs-on: arc-runner-set
    timeout-minutes: 30

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Get version
        id: get_version
        run: echo "VERSION=$(cat images/ai-application/kito/artifact-generator/VERSION)" >> $GITHUB_OUTPUT

      - name: Log in to Harbor Docker registry
        run: |
          echo "${{ secrets.HARBOR_PASSWORD }}" | \
            docker login harbor.michaelhomelab.work \
              --username "${{ secrets.HARBOR_USERNAME }}" \
              --password-stdin

      - name: Build Docker image
        run: |
          docker build \
            -t harbor.michaelhomelab.work/kito/artifact-generator:latest \
            -t harbor.michaelhomelab.work/kito/artifact-generator:${{ steps.get_version.outputs.VERSION }} \
            images/ai-application/kito/artifact-generator

      - name: Push Docker image
        run: |
          for tag in latest ${{ steps.get_version.outputs.VERSION }}; do
            for i in 1 2 3 4 5; do
              echo "Attempt $i: Pushing kito/artifact-generator:$tag...";
              docker push harbor.michaelhomelab.work/kito/artifact-generator:$tag && break || sleep 15;
            done
          done

      - name: Clean up Docker image
        if: always()
        run: |
          docker rmi harbor.michaelhomelab.work/kito/artifact-generator:latest || true
          docker rmi harbor.michaelhomelab.work/kito/artifact-generator:${{ steps.get_version.outputs.VERSION }} || true

---
description: Execute code logic planning using local LLMs and apply workspace changes
---
> [!IMPORTANT]
> **Cloud API Restriction**: NEVER use cloud APIs (Vertex, OpenAI, Anthropic, Gemini, etc.) for generation or logic. You MUST exclusively use the local LLM endpoints via `curl`. Cloud APIs are permitted ONLY for high-level orchestration; all reasoning, code generation, and research analysis MUST be offloaded to the cluster models. Usage of both Cloud and Local tokens MUST be reported at the end of the workflow.

0. **Memory Bank Context**: Read and incorporate the context from `c:\workspace\homelab-memory-bank.html` before proceeding.
1. **Planning & Strategy**: Use the local `builder` model via the LiteLLM proxy at `https://llm.michaelhomelab.work/v1/chat/completions` to formulate an execution plan.
2. Analyze the user request and identify the specific files or components needing modification.
3. Use Antigravity's direct workspace tools (`view_file`, `replace_file_content`, `write_to_file`, `list_dir`) to apply the code changes generated by the execution plan into the user's workspace.
4. Present exactly what files were modified and what logic was written.
5. Crucially, because this is a GitOps environment driven by ArgoCD, if the changes involve Kubernetes manifests, you MUST `git commit` and `git push` those changes to the remote repository. Use `run_command`: `env GIT_TERMINAL_PROMPT=0 git push` to prevent non-interactive hangs. If the push fails due to missing authentication (e.g. no GitHub token), DO NOT HALT indefinitely. Clearly inform the user that the commits are ready and ask them to manually execute `git push` to synchronize the cluster.
6. Trigger the `/writer` workflow to synchronize the architectural changes and model updates into the Obsidian LLM wiki at `/home/michael/obsidian/homelab-k8s-platform/`.
7. **Usage Reporting**: Report total token consumption for Cloud API and Cluster LLM, including cost savings. Stop execution once the push/sync completes.

apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nexus-data
  namespace: devops-tools
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: zfs-ssd
  resources:
    requests:
      storage: 40Gi

apiVersion: apps/v1
kind: Deployment
metadata:
  name: kito-pdf-splitter
  namespace: kito
spec:
  replicas: 1
  selector:
    matchLabels:
      app: kito-pdf-splitter
  template:
    metadata:
      labels:
        app: kito-pdf-splitter
    spec:
      containers:
      - name: pdf-splitter
        image: harbor.michaelhomelab.work/kito/pdf-splitter:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 5000
          name: http
        envFrom:
        - configMapRef:
            name: kito-config
        - secretRef:
            name: kito-slack-secrets
        resources:
          requests:
            cpu: 200m
            memory: 256Mi
          limits:
            cpu: 1
            memory: 512Mi
        livenessProbe:
          httpGet:
            path: /healthz
            port: 5000
          initialDelaySeconds: 30
          periodSeconds: 20
          timeoutSeconds: 5
          failureThreshold: 5
        readinessProbe:
          httpGet:
            path: /healthz
            port: 5000
          initialDelaySeconds: 20
          periodSeconds: 20
          timeoutSeconds: 5
          failureThreshold: 5

apiVersion: apps/v1
kind: Deployment
metadata:
  name: agent-ui
  namespace: coding-agent
spec:
  replicas: 1
  selector:
    matchLabels:
      app: agent-ui
  template:
    metadata:
      labels:
        app: agent-ui
    spec:
      containers:
        - name: agent-ui
          image: harbor.michaelhomelab.work/homelab/ai-agent/coding-agent/kodewriter-frontend:latest
          ports:
            - containerPort: 3000
---
apiVersion: v1
kind: Service
metadata:
  name: agent-ui
  namespace: coding-agent
spec:
  selector:
    app: agent-ui
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000

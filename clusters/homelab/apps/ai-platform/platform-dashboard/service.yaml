apiVersion: v1
kind: Service
metadata:
  name: platform-dashboard
  namespace: ai-platform
  labels:
    app: platform-dashboard
spec:
  type: ClusterIP
  selector:
    app: platform-dashboard
  ports:
    - name: http
      port: 80
      targetPort: http

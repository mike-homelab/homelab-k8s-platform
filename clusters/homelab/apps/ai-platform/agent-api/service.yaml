apiVersion: v1
kind: Service
metadata:
  name: agent-api
  namespace: ai-platform
  labels:
    app: agent-api
spec:
  type: ClusterIP
  selector:
    app: agent-api
  ports:
    - name: http
      port: 8000
      targetPort: http

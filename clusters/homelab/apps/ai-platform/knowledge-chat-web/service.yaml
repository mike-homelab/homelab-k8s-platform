apiVersion: v1
kind: Service
metadata:
  name: knowledge-chat-web
  namespace: ai-platform
  labels:
    app: knowledge-chat-web
spec:
  type: ClusterIP
  selector:
    app: knowledge-chat-web
  ports:
    - name: http
      port: 80
      targetPort: http

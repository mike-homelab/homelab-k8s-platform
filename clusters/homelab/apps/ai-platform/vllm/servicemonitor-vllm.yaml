apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: vllm-llm
  namespace: ai-platform
spec:
  selector:
    matchLabels:
      app: vllm-llm
  endpoints:
    - port: http
      path: /metrics
      interval: 30s

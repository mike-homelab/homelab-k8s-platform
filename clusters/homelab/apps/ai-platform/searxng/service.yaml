apiVersion: v1
kind: Service
metadata:
  name: searxng
  namespace: ai-platform
  labels:
    app: searxng
spec:
  ports:
  - port: 8080
    targetPort: 8080
    name: http
  selector:
    app: searxng

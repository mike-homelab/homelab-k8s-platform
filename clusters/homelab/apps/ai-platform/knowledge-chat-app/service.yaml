apiVersion: v1
kind: Service
metadata:
  name: knowledge-chat-app
  namespace: ai-platform
  labels:
    app: knowledge-chat-app
spec:
  type: ClusterIP
  selector:
    app: knowledge-chat-app
  ports:
    - name: http
      port: 80
      targetPort: http

apiVersion: v1
kind: Service
metadata:
  name: opensearch-dashboards-lb
  namespace: monitoring
spec:
  type: LoadBalancer
  selector:
    app: opensearch-dashboards
  ports:
    - name: http
      port: 5601
      targetPort: 5601

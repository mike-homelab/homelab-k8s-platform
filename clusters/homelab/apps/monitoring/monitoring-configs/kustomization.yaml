resources:
  - namespace.yaml
  - ../ai-inference-dashboard-providers.yaml
  - grafana-deployment-patch.yaml

patchesStrategicMerge:
  - grafana-deployment-patch.yaml

  - ai-inference-dashboard-configmap.yaml


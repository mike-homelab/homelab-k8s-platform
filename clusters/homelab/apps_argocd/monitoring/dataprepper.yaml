apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: dataprepper
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "12"
spec:
  project: monitoring

  source:
    repoURL: https://opensearch-project.github.io/helm-charts
    chart: data-prepper
    targetRevision: "0.3.1"
    helm:
      values: |
        replicaCount: 1

        pipelineConfig:
          enabled: false
          existingSecret: dataprepper-pipelines

        resources:
          requests:
            cpu: 250m
            memory: 512Mi
          limits:
            memory: 1Gi

<<<<<<< HEAD
=======
        livenessProbe:
          httpGet:
            path: /health
            port: 4900
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 2
          failureThreshold: 3

        readinessProbe:
          httpGet:
            path: /health
            port: 4900
          initialDelaySeconds: 20
          periodSeconds: 10
          timeoutSeconds: 2
          failureThreshold: 3

        service:
          ports:
            - name: prom-remote-write
              port: 2021
              targetPort: 2021
            - name: otlp-grpc
              port: 21890
              targetPort: 21890

>>>>>>> e677b84ef3755e2357cdd2feea5a5c0a22a63849
        extraEnvs:
          - name: OPENSEARCH_PASSWORD
            valueFrom:
              secretKeyRef:
                name: opensearch-admin
                key: password

        volumes:
          - name: opensearch-ca
            secret:
              secretName: opensearch-tls

        volumeMounts:
          - name: opensearch-ca
            mountPath: /usr/share/data-prepper/config/opensearch-ca.crt
            subPath: ca.crt
            readOnly: true

  destination:
    server: https://kubernetes.default.svc
    namespace: monitoring

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
